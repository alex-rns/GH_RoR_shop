<div class="row">
  <div class="dropdown">
    <a class="btn dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
       data-toggle="dropdown" aria-expanded="false">
      sort
    </a>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
      <%= link_to 'Expensive first', category_products_path(sort: 'expensive', category_id: @category ), class:'dropdown-item' %>
      <%= link_to 'Cheapest first', category_products_path(sort: 'cheapest', category_id: @category ), class:'dropdown-item' %>
    </div>
  </div>
  <%= render @products %>
  <%= will_paginate @products, class: "pagination" %>
</div>