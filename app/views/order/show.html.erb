<h2 class="text-center mt-5">Cart</h2>
<% if @items.empty? %>
  <h4>Cart is empty</h4>
<% else %>
  <div id="cart">
    <% @items.each do |item| %>
      <%= render 'order_items/order_item', item: item %>
    <% end %>
  </div>
  <%= render 'total_order_price' %>
<% end %>